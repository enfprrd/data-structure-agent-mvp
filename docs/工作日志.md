# 数据结构 Agent MVP 工作日志

> 用途：简要记录每一次主要改动，便于后续复盘、写报告、写论文或准备答辩材料。

## 2026-06-01 到 2026-06-02：项目雏形

### a8e3d6e initial data structure agent mvp

- 搭建 Streamlit 问答页面。
- 接入 DeepSeek Chat API。
- 建立本地 Markdown 知识库检索雏形。
- 支持基础数据结构问答和 C 语言代码生成。
- 建立早期演示流程和运行脚本。

### 80da484 improve mobile layout

- 调整页面在移动端的显示方式。
- 优化聊天区和演示区的基本布局。
- 让 MVP 页面更适合手机和窄屏浏览。

### 51e1c59 refactor visualization flow

- 重构右侧演示流程。
- 将演示请求、模拟器、渲染器拆分为更清晰的模块。
- 引入统一的可视化步骤结构，为后续多种数据结构演示做准备。

### 454a2ef align demo parsing with generated code

- 强化 DeepSeek 输出答案和演示 JSON 的一致性。
- 要求演示类回答末尾生成完整可运行 C 程序。
- 让本地演示解析优先依据 C 代码中的初始数据和操作参数。

## 2026-06-03：知识库扩展与演示范围扩大

### 9dca89e expand data structure knowledge and demos

- 从教材 OCR 文本整理整门本科数据结构知识库。
- 修复原 knowledge 中文乱码。
- 新增或重写多个章节笔记，包括绪论、线性表、栈和队列、串、数组、树、图、查找、排序等。
- 将应用范围从“第 2 章线性表”扩展为整门数据结构课程。
- 修改系统提示词、页面说明和 README。
- 扩展 RAG 领域关键词，不再只偏向线性表。

### 07651e9 add tree graph search sort demos

- 新增二叉树演示器。
- 新增图演示器。
- 新增查找表演示器。
- 新增排序演示器。
- 支持二叉树先序、中序、后序、层序遍历。
- 支持图 build、DFS、BFS、Dijkstra。
- 支持顺序查找、折半查找。
- 支持冒泡、插入、选择、快速排序。
- 修复“翻教材中...”可能卡住的问题，加入 45 秒 stale 自动解锁和侧边栏恢复按钮。

## 2026-06-04：检索质量、部署、栈应用和绘图增强

### ca82ddc Added Dev Container Folder

- 添加 Dev Container 相关配置。
- 方便后续在统一开发环境中运行项目。

### 6c215de 修改knowledge匹配规则

- 修复“当前问题 + 完整历史对话”污染 RAG 检索的问题。
- RAG 检索改为优先依据当前问题。
- 历史对话仍保留给 DeepSeek 作为聊天上下文。
- 增加中文常见虚词过滤。
- 强化标题命中和章节归属权重。
- 每个 Markdown 小节检索时带上所属章标题。
- 短语匹配忽略空格，支持 `B树` 匹配 `B 树`。
- 将“和/与”归一，支持 `B树和B+树` 匹配 `B 树与 B+ 树`。
- 增加 `顺序表与链表`、`外排` 等领域词。
- 在 `external_sorting.md` 中补充“败者树”小节。
- 将版本号改为按项目关键文件最新修改时间生成。

### 0129ec5 add stack application demos

- 大幅扩展栈演示能力。
- 新增 `visualizer/simulators/stack_applications.py`。
- 支持链栈 push/pop、进制转换、括号匹配、中缀转后缀、后缀表达式求值、前缀表达式求值、表达式求值。
- 支持函数调用栈、递归调用、汉诺塔递归和非递归模拟。
- 支持迷宫回溯、非递归 DFS、二叉树非递归先序/中序/后序遍历。
- 支持浏览器前进后退、撤销重做、标签匹配、语法分析栈。
- 支持单调栈、下一个更大元素、柱状图最大矩形、每日温度。
- 支持栈排序、两个栈实现队列、栈逆序、火车调度、出栈序列合法性判断。
- 在知识库 `stack_queue.md` 中补充“栈的经典应用”。
- 在 DeepSeek 意图解析词表中加入栈应用操作。
- 新增栈应用相关测试。

### 3c8ff5a improve responsive demo layout

- 优化 HD 屏和手机布局。
- 演示板改为独立滚动区域，避免长演示撑坏整页。
- 聊天区也改为独立滚动。
- 小高度桌面屏自动压缩标题和面板高度。
- 手机端限制演示板高度并允许内部滚动。
- 小屏下缩小演示元素，减少横向溢出。

### ae901b3 render tree and graph with svg layouts

- 二叉树改用 SVG 坐标布局绘制。
- 每个二叉树节点有中心点坐标。
- 父子连线按中心点计算，并避开节点圆心。
- 支持 `None/null` 空节点虚线显示。
- 图改为 SVG 圆形布局。
- 图顶点之间绘制真实边，不再只是孤立顶点列表。
- 图边支持箭头和权值标签。
- 新增渲染器测试，验证二叉树空节点、连线和图边权值。

### 15c9ca8 fix follow-up demo retrieval and add merge sort

- 发现“归并算法复杂度是多少”后追问“演示一下呗”会跑到链表。
- 初步修复省略主语追问：追问会继承上一条用户主题。
- 增加 `归并算法 -> 归并排序` 的检索别名。
- 新增 `merge_sort` 到排序演示器。
- 支持归并排序分治、二路归并、辅助数组写回过程演示。
- 新增归并排序测试。

### 19fda17 use llm to rewrite ambiguous followups

- 进一步修复追问处理策略。
- 不再用过多硬编码规则猜测“演示一下呗”的主题。
- 对含糊短追问，优先调用 DeepSeek 根据最近对话改写成独立检索问题。
- DeepSeek 改写失败时才使用本地兜底。
- 新增测试验证含糊追问会采用 LLM 改写结果。

## 当前验证状态

- 本地已安装项目依赖并运行 pytest。
- 最近一次测试结果：`39 passed`。
- 主要验证范围：
  - 协议校验。
  - 顺序表、单链表、栈、队列演示。
  - 栈应用演示。
  - 归并排序演示。
  - 二叉树和图 SVG 渲染。
  - 含糊追问改写。

## 2026-06-05：登录历史与图演示增强

### 85be153 add login and conversation history

- 新增账号密码注册和登录。
- 新增 SQLite 对话历史。
- 未登录用户通过 cookie/device_id 保存当前设备历史。
- 登录后将当前设备绑定到账号。
- 新增 `auth_store.py` 管理用户、设备、会话和消息。
- 将 `data/` 加入 `.gitignore`，避免提交用户历史和密码哈希。
- 新增工作日志和实现笔记。

### 图相关演示增强

- 根据 `knowledge/graph.md` 中的教材内容，补充 Prim、Kruskal、Floyd、拓扑排序演示。
- Prim 用已选顶点集合逐步选择跨集合最小边。
- Kruskal 用边权排序和并查集思想判断是否成环。
- Floyd 用距离矩阵展示逐步允许中间顶点后的松弛过程。
- 拓扑排序展示入度计算、入度为 0 顶点输出和删除出边过程。
- 无向图 SVG 渲染不再显示箭头，最小生成树演示更贴近教材定义。
- 新增图算法测试。

### RAG 关键词记忆增强

- 含糊追问不再由本地拼接上一轮主题，DeepSeek 不可用时直接使用原问题。
- 每轮回答后由 DeepSeek 总结隐藏 RAG 关键词，保存到对话消息中。
- 下一轮检索同时使用当前有效问题、RAG 关键词、二者组合结果，并对关键词命中片段加权。
- 针对“图 DFS”后追问“更多的结点”补充测试，避免跑偏到链表结点。
- 最近一次测试结果：`44 passed`。

### 图顶点标签解析约束

- 发现图 DFS 演示中，用户给出数字顶点 `1,2,3,4,5` 时，右侧可能显示为 `A,B,C,D,E`。
- 不采用本地从原文识别数字并替换标签的方案，避免规则修补引入新歧义。
- 改为在 DeepSeek 演示意图解析 prompt 中明确要求：图顶点标签必须原样保留，邻接矩阵必须写入 `metadata.vertices`。
- 明确要求 DFS/BFS 起点标签和 `metadata.vertices` 一致，无权图不要凭空生成边权，邻接表逗号后的数字表示邻接点而不是权值。
- 保留本地测试验证：只要 OperationRequest 给出数字顶点，本地模拟器和渲染器会原样使用。

### 排序指针/游标演示增强

- 顺序数组渲染器支持显示 `state.pointers`，可在数组格子上方展示变量标签，并在指针面板列出变量指向位置。
- 冒泡排序补充 `i`、`i+1`、`end` 游标。
- 插入排序补充 `i`、`j`、`insert` 游标。
- 选择排序补充 `i`、`j`、`min` 游标。
- 快速排序补充 `low`、`high`、`i`、`j`、`pivot`，展示分区时左右扫描和枢轴归位。
- 归并排序补充 `left`、`mid`、`right`、`i`、`j`、`k`，展示两段归并和写入辅助数组的位置。
- 新增排序指针渲染测试。
- 最近一次测试结果：`46 passed`。

### 知识库阅读入口

- 在 sidebar 新增“知识库阅读”入口。
- 用户可以选择 `knowledge/` 中的章节文件并按 Markdown 小节展开阅读。
- 支持在知识库中输入关键词搜索相关小节。
- 保留“本次检索到的知识库片段”展示，方便用户对照本轮回答依据。
- 新增知识库切分和搜索测试。
- 最近一次测试结果：`48 passed`。

### 主界面整理

- 将主页面整理为“问答演示”和“知识库阅读”两个页签。
- “问答演示”页签保留聊天区、当前演示区和本次检索资料片段。
- “知识库阅读”页签独立承载章节选择、小节展开和关键词搜索。
- 侧边栏瘦身，只保留运行状态、账号、上下文状态、恢复生成和清空对话。
- 移除侧边栏中的示例问题、RAG 关键词调试展示和知识库阅读折叠区。
- 最近一次测试结果：`48 passed`。

## 当前已推送 GitHub 状态

- 最新提交：`a2a11d0 add graph algorithm demos`
- 分支：`main`
- 远端：`git@github.com:enfprrd/data-structure-agent-mvp.git`

## 2026-06-08：问答界面重构、演示同步和 RAG 检索策略修正

### 开工前约定

- 以后每次继续本项目任务前，必须先阅读：
  - `docs/工作日志.md`
  - `docs/实现笔记.md`
- 阅读目的：
  - 先理解已有架构和用户已经定下的策略。
  - 避免重复引入本地硬编码规则。
  - 避免把已经决定交给 DeepSeek 的判断逻辑又写成本地 if/else。
  - 避免覆盖用户已有改动。

### 问答界面重构

- 将第一个问答界面改为现代左右双栏布局：
  - 左侧为“自由提问”聊天区。
  - 右侧为“演示板”。
  - 中间使用浅灰分隔线。
- 聊天消息改为气泡形式：
  - 用户消息右侧蓝色气泡。
  - 助手消息左侧浅灰气泡。
  - 保留 Markdown 渲染能力。
- 输入区从页面流中拆出，固定为左栏底部的独立输入栏。
- 消息列表改为独立滚动区，避免长回答把输入框顶走。
- 右侧演示板保留独立滚动和步骤控制。
- 曾经为追求“全屏两栏”误隐藏侧边栏，后恢复侧边栏状态、账号、清空、恢复、薄弱点等功能。

### 演示板和历史消息同步

- 修复左侧历史消息中出现“同步演示步骤”，但右侧演示板仍显示等待态的问题。
- 新生成的助手消息会保存对应 `demo_request`。
- 点击历史消息中的步骤时，会从消息恢复 OperationRequest，重新 dispatch 生成右侧演示轨迹，并跳到对应步骤。
- 旧历史消息由于之前没有保存 `demo_request`，无法可靠恢复演示；当右侧没有当前演示数据时，会隐藏这些旧步骤入口，避免误导。
- 将“同步演示步骤”按钮样式从大面积主色按钮改为浅色列表按钮，避免大块紫/蓝色污染聊天区。

### RAG 检索策略回滚到 DeepSeek 规划

- 发现“演示二叉树的后序遍历”曾被上一轮“顺序表”相关 RAG 关键词带偏，回答为“知识库资料不足”。
- 临时尝试过本地主题过滤表，但这违背了既定策略，已撤掉。
- 当前策略改为：
  - 用户提问后，先让 DeepSeek 输出本轮 RAG 检索计划。
  - 检索计划包含：
    - `query_keywords`
    - `groups`
    - 每组 `weight`
    - 仍相关的 `history_keywords`
  - 当前问题关键词权重最高。
  - DeepSeek 分组关键词按组权重参与检索。
  - 历史 RAG 关键词只有在 DeepSeek 判断仍相关时，才以低权重辅助。
- `rag.py` 增加 `boost_weights`，让权重直接进入关键词检索评分。
- 不再使用本地 `RAG_TOPIC_GROUPS` / `_matched_rag_topics` / `filter_rag_keywords_for_question` 这类本地主题判断。

### 测试状态

- 新增/更新 `tests/test_rag_memory.py`：
  - 验证短追问可通过 RAG plan 继承图主题。
  - 验证“后序遍历”在当前 RAG plan 指向二叉树时，不会被历史“顺序表/插入”带偏。
- 最近一次完整测试结果：`50 passed`。

### 侧边栏与小测交互修正

- 发现隐藏 Streamlit toolbar 会影响侧边栏折叠后的展开入口。
- 改为不隐藏整个 toolbar，只隐藏 Deploy/header 普通按钮，并显式保留 `headerNoPadding` / `stExpandSidebarButton` 这类侧边栏控制按钮。
- 已用 Playwright 实测：侧边栏可以收起，也可以重新展开。
- 随堂小测和主线学习小测的 `st.radio` 改为默认不选中任何答案。
- 小测只有在用户实际选择后才记录答案、显示正确/错误反馈，避免一打开就默认选中第一项。

### GitHub SSH 推送准备

- 按项目约定补充本次工作日志。
- 确认当前仓库远端为 SSH 地址：`git@github.com:enfprrd/data-structure-agent-mvp.git`。
- 准备提交并推送本轮问答界面、课程主线、知识库阅读、RAG 检索计划和图渲染相关改动。
- 将 `.playwright-cli/` 和 `output/` 运行验证产物加入 `.gitignore`，避免把本地临时截图和日志推送到 GitHub。

## 2026-06-09：动画演示改为点击弹窗展示

### 演示区交互收敛

- 将右侧常驻演示板改为轻量摘要入口，只显示演示标题、当前步骤和当前步骤解释。
- 完整动画画布、播放控制、步骤滑块和步骤列表移入 `st.dialog` 弹窗中。
- 回答消息中的“同步演示步骤”改为“打开演示步骤”，点击后会恢复对应演示、定位到目标步骤并打开弹窗。
- 主线学习的“加载本课演示”和错题复习的“跳到相关演示步骤”也改为直接打开弹窗。
- 保留每一步解释：右侧摘要提供折叠的“每一步解释”，弹窗中继续保留当前步骤说明和步骤列表。
- 默认页面不再渲染完整动画 HTML，减少不必要的常驻渲染开销，使问答区排版更简洁。
- 本地验证：`python -m pytest` 通过，结果为 `50 passed`；使用 Playwright CLI + Edge 截图确认问答页默认布局正常。

### 根据交互意图再次修正

- 明确用户不是要在右侧演示板中决定是否演示，而是要整个问答页保持纯对话框形态。
- 移除问答页右侧常驻演示板，问答页改回单栏对话容器。
- 助手回复中直接提供“打开动画演示”按钮；点击后恢复该回复保存的 `demo_request`，并在弹窗中展示完整动画。
- 保留“选择具体步骤”折叠入口，用户可以从回复中直接定位到某一步并打开弹窗。
- 删除不再使用的演示板摘要渲染函数和摘要样式，避免代码继续围绕右侧演示板布局组织。
- 单栏后移除旧的固定输入栏绝对定位规则，改为标题、对话滚动区、输入区的稳定文档流布局。
## 2026-06-09：新增 PPT-Aware Learning 第一阶段 MVP

- 新增 `ppt_learning.py`，定义 `SlideCard` 与 `ContextPack`，把 PPTX 解析、页感知检索、Prompt 构造和 DeepSeek 回答封装为独立模块。
- 使用 `python-pptx` 本地解析 PPTX：提取每页标题、文本框、表格、备注 notes、页码 slide_id，并为每页生成 SlideCard。
- 对有文本的页面生成本地兜底 summary/keywords/concept_type；DeepSeek 可用时逐页补充元数据，只发送单页文本，不发送整份 PPT。
- 对无可提取文字的页面标记 `text_missing`，明确不猜测图片内容，第一阶段不做 OCR 和视觉理解。
- 新增 page-aware RAG：当前页完整 SlideCard、前后页摘要、整份目录、问题相关页、当前 PPT 对话摘要，并可补充少量本地教材知识库片段。
- Streamlit 新增“PPT学习模式”：支持上传 PPTX 或输入本地 PPTX 路径，左侧阅读当前页提取文本/备注/表格，右侧保留 PPT 独立对话框与“讲解当前页”按钮。
- 回答 Prompt 明确要求优先依据当前页，其次前后页和相关页，再参考教材；PPT 无相关信息时必须说明“不在当前 PPT 文本中”，并标注来源页码。
- 新增 `tests/test_ppt_learning.py` 覆盖 PPT 文本/表格/备注解析、空白页 `text_missing`、ContextPack 页感知上下文。
- 依赖新增 `python-pptx>=1.0.2`。
- 本地验证：`python -m py_compile app.py ppt_learning.py` 通过；`python -m pytest` 通过，结果为 `53 passed`。

### 运行环境依赖补装

- 发现 Streamlit 实际使用的是 `F:\Conda` 环境，而此前 `python-pptx` 安装到了 `C:\Python314`。
- 已使用 `F:\Conda\python.exe -m pip install "python-pptx>=1.0.2"` 将依赖安装到 Streamlit 运行环境。
- 验证 `F:\Conda\python.exe -c "import pptx; print(pptx.__version__)"` 输出 `1.0.2`。
- 验证 `F:\Conda\python.exe -m pytest tests/test_ppt_learning.py -q` 通过，结果为 `3 passed`。

### PPT 学习模式对话区滚动修正

- 将右侧“AI讲解与答疑”的消息列表包入 `ppt_chat_scroll` 容器。
- 为 PPT 对话区增加固定高度、内部滚动、滚动条预留和移动端高度约束，避免 AI 讲解越多页面越往下撑。
- 验证 `python -m py_compile app.py` 通过。
- 验证 `python -m pytest tests/test_ppt_learning.py -q` 通过，结果为 `3 passed`。

### PPT 原图预览与内部文本分离

- 调整 PPT 学习模式：用户阅读区优先展示本地渲染出的 PPT 页 PNG，解析出来的文字改为折叠的“内部解析数据”。
- 新增 `render_pptx_slide_images()`，在 Windows 本地通过 PowerPoint COM 将 PPTX 每页导出为 PNG，缓存到 `data/ppt_previews/{deck_id}/slide_XXX.png`。
- 明确该 PNG 只用于前端展示，不会传给 DeepSeek；AI 仍然只基于 `SlideCard` 文本、备注、表格和 ContextPack 回答。
- 如果当前环境无法本地渲染 PPT 图片，会提示“暂时无法渲染 PPT 原图预览”，但不影响文本版 AI 答疑。
- 用临时 PPTX 验证本地 PowerPoint 导出链路可生成 `slide_001.png`。
- 验证 `F:\Conda\python.exe -m py_compile app.py ppt_learning.py` 通过。
- 验证 `F:\Conda\python.exe -m pytest -q` 通过，结果为 `53 passed`。
## 2026-06-11：模块边界梳理

- 目前项目可以先切成 6 个主要模块：
  - 界面与交互层：`app.py` 里的 Streamlit 页面、侧边栏、对话区、PPT 学习页、知识库页。
  - 对话与会话层：账号、设备、会话、消息历史、恢复/清空状态，主要由 `auth_store.py` 和 `app.py` 的 session 管理承担。
  - 知识检索层：`rag.py` 和 `knowledge/`，负责 Markdown 片段检索、关键词加权、上下文补充。
  - 通用教学生成层：DeepSeek 调用、prompt 组装、回答后处理、代码校验，这一层最适合抽成共享基类。
  - DSVP 演示层：`visualizer/` 整套协议、意图解析、分发器、模拟器、渲染器，负责把自然语言转成可视化步骤。
  - PPT 学习层：`ppt_learning.py`，负责 PPTX 解析、页卡建模、上下文打包、按页回答、预览图导出。
- 你提到的“对话框”和“PPT 学习的 AI 讲解”确实可以共用一个父类，建议抽成“教学型回答服务”基类，统一处理：
  - 读取上下文
  - 组织 prompt
  - 调 DeepSeek
  - 做回答后处理
  - 生成结构化附加数据
- 这样拆完以后，`app.py` 更像调度器，不再承担太多业务逻辑。
## 2026-06-11：公共历史上下文工具合并

- 新增 `conversation_context.py`，把“对话历史格式化”和“对话历史压缩摘要”合并到同一套公共工具里。
- `app.py` 不再自己实现消息历史拼接，而是调用公共工具后再保留一个很薄的兼容封装。
- `ppt_learning.py` 的会话摘要也改为复用同一套公共工具，减少了两份几乎一致的历史文本处理逻辑。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11：弹窗高度与按钮参数修正

- 将代码里的 `use_container_width` 统一替换为新版 `width` 参数，消除 Streamlit 未来弃用告警。
- 给演示弹窗增加内部滚动壳，避免演示步骤、说明和控制区把对话框撑得过高。
- 目标是让弹窗保持“宽一些、但不要太高”，把视线集中在当前演示步骤上。
## 2026-06-11：演示弹窗高度收紧

- 给演示弹窗内部加了独立滚动壳，避免步骤区和控制区把对话框撑到过高。
- 保留弹窗大宽度，主要压缩的是垂直方向的占用，让内容更像真正的模态窗口。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11：聊天输入框底部固定修正

- 将聊天页输入区从绝对定位改为正常文档流中的底部区域，避免它在不同屏幕高度下漂移。
- 让消息区承担中间滚动空间，输入区始终留在聊天容器的最下端。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11：聊天输入改用原生底部控件

- 把问答页输入框改回 `st.chat_input`，让 Streamlit 自己负责贴底布局。
- 删除了把 `st.chat_input` 强行改成静态定位的冲突样式，避免它再次离开底部。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11：输入框移出聊天容器

- 将 `st.chat_input` 从聊天内容容器中移出，改为页面级输入控件。
- 这样输入框不再参与聊天内容的布局计算，避免它被内容区挤到中间。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11：问答页演示改为弹窗

- 将问答页从“左侧对话 + 右侧演示板”的双栏布局，收敛成单栏聊天页面。
- 新增 `st.dialog` 弹窗承载演示内容，聊天消息里的演示入口现在会直接打开弹窗。
- 保留原有 DSVP 演示数据结构、步骤切换和自动播放逻辑，只改展示方式，不改核心演示引擎。
- 回归验证结果：`python -m pytest -q` 通过，`53 passed`。
## 2026-06-11锛氶〉闈㈡粴鍔ㄦ竻鐞嗕笌浣欐瘡浠ｇ爜鍓悊

- 鍒犻櫎 `app.py` 里旧的移动端聊天滚动偏移规则，去掉 `chat_scroll` 的 `top/bottom` 绝对偏移和固定高度残留。
- 保留当前真正还在用的响应式规则，只留下列宽换行和气泡宽度控制，避免旧布局和正常文档流滚动相互干扰。
- 已用 `python -m py_compile app.py` 验证语法通过。

## 2026-06-11：对话框参考 DeepSeek 样式重做

- 实际抓取 `https://chat.deepseek.com` 截图，对照其居中、轻量、低边框的登录/对话视觉，而不是继续凭感觉改样式。
- 用 Playwright + 本机 Edge 截取当前本地页面，确认问答页输入框过宽、助手回答灰底过重、演示入口藏在折叠项里。
- 将问答内容区收窄居中，欢迎态去掉虚线卡片，输入框改成居中的圆角长条，助手回答改为更轻的纯文本气泡。
- 将“打开动画演示”从折叠项里提出来做成消息下方直接可点的主按钮，具体步骤仍保留在折叠项中。
- 根据二次截图继续收紧聊天回答标题字号，移除演示弹窗底部整条关闭按钮，保留右上角关闭入口，让弹窗更像轻量模态窗口。
## 2026-06-11：滚轮、小测和演示弹窗联动修复

- 修复问答主页面滚轮问题：移除残留的 `.st-key-chat_scroll` 固定高度和内部滚动规则，让 Streamlit 主内容容器承接滚轮滚动，避免鼠标必须落进某个内层滚动盒。
- 修复随堂小测答案总在 A 的问题：保留原题库内容，但根据题目和正确答案生成稳定随机种子，打散选项顺序，刷新后不乱跳，也不再固定第一项正确。
- 修复点击小测选项把演示弹窗重新弹出的问题：`demo_dialog_open` 改为一次性打开状态，渲染弹窗后立即消费；只有弹窗内部播放、上一/下一步、滑块和步骤跳转会重新续开弹窗。
- 保留答错后的“跳到相关演示步骤”作为明确打开弹窗入口，避免普通单选触发页面刷新时误开演示。
- 验证结果：`python -m py_compile app.py` 通过；浏览器实测鼠标滚轮可驱动主内容容器滚动，点击小测选项后 `dialogAfterOption=false`，弹窗未复活；小测样例正确答案位置已不固定为第一项。
## 2026-06-12：PPT 讲解与答疑缩小版主页面化

- 将 PPT 学习模式右侧标题从“AI讲解与答疑”改为“讲解与答疑”，去掉界面里的 AI 字样。
- 复用主页面消息渲染能力，让 PPT 右侧消息支持用户/助手气泡、C 语言代码折叠、代码检查折叠、随堂小测和动画演示入口。
- 给 PPT 消息渲染增加独立 `ppt_` key 前缀，避免 PPT 小测、演示按钮和主页面状态互相串联。
- PPT 回答生成后会基于 PPT 页上下文生成小测、代码检查摘要、可折叠代码块，并尝试解析演示请求；解析成功时可从消息中打开同一套演示弹窗。
- PPT 右侧聊天区补充内部滚动样式，形成主页面的缩小版聊天窗口，同时消息来源仍然来自当前 PPT 页、相关页和 PPT 对话历史。
- 验证结果：`python -m py_compile app.py` 通过；当前仓库未发现可直接用于浏览器验收的 PPTX 文件，因此本轮完成代码级验证。
## 2026-06-12锛歅PT 瀛︿範椤靛姞鍏ユ湰鍦扮浠?

- 鍦?PPT 瀛︿範妯″紡涓柊澧?`localppt/` 鍏ュ彛锛岀洿鎺ヨ鍙栨湰鍦颁笅杞界殑 `.pptx` 课件銆?
- 澧炲姞鏈湴璇剧▼搴撻€夋嫨涓嬫媺妗嗭紝鐢ㄦ埛涓嶅啀闇€瑕佹墜鍔ㄨ緭鍏ュ畬鏁磋矾寰勶紝鍙洿鎺ヤ粠鏂囦欢鍚嶇偣寮€銆?
- 鏂板鏈湴 PPTX 鍒楄〃鎺㈡煡鍑芥暟鍜屽皬娴嬶紝纭繚鍙 .pptx 鏂囦欢鏀跺綍锛屽苟涓嶅奖鍝嶇幇鏈夌殑涓婁紶鍜屾湰鍦拌矾寰勬ā寮忋€?
## 2026-06-12锛氱鐐硅Е鍙戠殑 1 姝ユ紨绀烘粴鍧楀紓甯稿苟淇

- 淇婕旂ず寮圭獥涓殑姝ラ婊氬姩鏉匡細褰撴紨绀哄彧鏈?1 涓楠ゆ椂涓嶅啀娓叉煋 `st.slider`锛屽厤寰楀湪 `min_value == max_value` 鏃跺彂鐢?StreamlitAPIException銆?
- 鍦?1 姝ョ殑鎯呭喌涓嬶紝鐩存帴鏄剧ず鍥哄畾鎻愮ず锛屾紨绀哄缁堝彲璇诲彇銆佷笉浼氬啀鎶撳姞杞芥椂寮傚父銆?
- 宸茶繘琛屾湰鍦板厠闄?`python -m py_compile app.py`楠岃瘉閫氳繃銆?
